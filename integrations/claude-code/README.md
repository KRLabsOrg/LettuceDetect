# Claude Code Hook: Hallucination Detector

This hook uses the HallucinationDetector to predict hallucinations in agent answers and surfaces flagged spans as hook feedback.

## Installation

1. Clone the repository.
2. Install the required dependencies.
3. Configure the hook settings in `settings.json`.

## Configuration

Create a `settings.json` file with the following settings: